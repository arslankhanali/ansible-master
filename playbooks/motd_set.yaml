---
- name: Add lines to message of the day (motd)
  hosts: all
  become: true
  tasks:
    - name: Set up welcome message
      ansible.builtin.lineinfile:
        path: /etc/motd
        line: "{{ welcome_message | default('Savvy!') }}"
        state: present
